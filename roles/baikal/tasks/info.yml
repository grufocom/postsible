---
# Display Baïkal information

- name: Display Baïkal information
  debug:
    msg:
      - "Baïkal CalDAV/CardDAV Server Installation Complete!"
      - ""
      - "Version: {{ baikal_version }}"
      - ""
      - "Web Interface:"
      - "  Admin URL: https://{{ baikal_domain }}{{ baikal_url_path }}/admin/"
      - "  Username: admin"
      - "  Password: (set from vault_baikal_admin_password)"
      - ""
      - "CalDAV URL:"
      - "  https://{{ baikal_domain }}{{ baikal_url_path }}/cal.php/principals/USERNAME/"
      - ""
      - "CardDAV URL:"
      - "  https://{{ baikal_domain }}{{ baikal_url_path }}/card.php/addressbooks/USERNAME/default/"
      - ""
      - "Database (Shared with Mailserver):"
      - "  Name: {{ baikal_db_name }}"
      - "  User: {{ mariadb_user }}"
      - "  Note: Uses the same database as Postfix/Dovecot"
      - ""
      - "Installation path: {{ baikal_install_path }}"
      - "Data path: {{ baikal_data_path }}"
      - ""
      - "Client Configuration:"
      - "  iOS/macOS: Add Account → Other → CalDAV/CardDAV"
      - "  Android: Install DAVx⁵ from F-Droid/Play Store"
      - "  Thunderbird: Install Lightning + SOGo Connector"
      - ""
      - "Next Steps:"
      - "  1. Login to admin interface"
      - "  2. Create user accounts"
      - "  3. Configure clients"
      - ""
      - "Important Notes:"
      - "  - Baïkal tables are in the mailserver database"
      - "  - Backup mailserver database includes Baïkal data"
      - "  - One database to rule them all! 🎯"
  tags: baikal
